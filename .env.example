PORT=4000
NODE_ENV=development
DATABASE_URL="your db connection string here, e.g., postgresql://user:password@host:port/database?sslmode=require&channel_binding=require"
JWT_SECRET="replace-with-a-strong-secret"
JWT_EXPIRES_IN="1d"
